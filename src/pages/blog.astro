---
import Header from "../components/Header.astro";
import GoogleAnalytics from "../components/GoogleAnalytics.astro";
import Footer from "../components/Footer.astro";
import Icon from "../components/Icon.astro";
const lang = 'es';
const currentPath = Astro.url.pathname;
import "../styles/global.css";
---

<!doctype html>
<html lang="es">
  <head>
    <GoogleAnalytics />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog - Escala 24x7</title>
    <link rel="icon" type="image/png" href="/logos/escala-webclip.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght@400&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <Header lang={lang} currentPath={currentPath} />
    <main>
      <!-- Hero Section -->
      <section class="hero-section">
        <div class="hero-wrapper">
          <div class="hero-banner">
            <div class="hero-container">
              <div class="hero-content">
                <h1 class="hero-title">Blog Escala 24x7</h1>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Entradas del blog -->
      <section class="blog-entries-section">
        <div class="blog-entries-container">
          <a href="/blog/nueva-imagen-escala24x7" class="blog-entry-card">
            <div class="blog-entry-image">
              <img src="/images/blog/blog-image-1.jpg" alt="Escala 24x7 integración Grupo Stefanini" />
            </div>
            <div class="blog-entry-body">
              <span class="blog-entry-date">Enero 26, 2026</span>
              <h2 class="blog-entry-title">Escala 24x7 consolida su integración al Grupo Stefanini con la evolución de su identidad corporativa.</h2>
              <p class="blog-entry-excerpt">Tras el hito que supuso su alianza con el Grupo Stefanini a principios de este año, Escala 24x7 da un paso firme en su estrategia de consolidación regional al presentar la evolución de su identidad corporativa.</p>
              <div class="blog-entry-cta">
                <span class="btn btn-secondary">
                  Lee el artículo completo
                  <Icon name="north_east" size={20} />
                </span>
              </div>
            </div>
          </a>
          <!-- Más entradas se añadirán aquí -->
        </div>
      </section>
    </main>
    <Footer lang={lang} />
  </body>
</html>

<style>
  /* Hero Section */
  .hero-section {
    position: relative;
    width: 100%;
    background-color: #020E1E;
  }

  .hero-wrapper {
    padding: 0;
    height: 50vh;
    min-height: 400px;
  }

  .hero-banner {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    background-image: url("/images/blog/blog-hero.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    display: flex;
    align-items: center;
    padding-top: 60px;
  }

  .hero-container {
    position: relative;
    z-index: 2;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 100px;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-content {
    max-width: 800px;
    text-align: center;
  }

  .hero-title {
    font-family: 'Nexa', sans-serif;
    color: var(--neutral-100);
    font-size: 64px;
    font-weight: 700;
    line-height: 1.1;
    margin: 0 0 24px 0;
  }

  .hero-subtitle {
    font-family: 'Nexa', sans-serif;
    color: var(--neutral-100);
    font-size: 32px;
    font-weight: 500;
    line-height: 1.3;
    margin: 0 0 24px 0;
  }

  .hero-description {
    font-family: 'Infra', sans-serif;
    color: var(--neutral-100);
    font-size: 18px;
    font-weight: 400;
    line-height: 1.6;
    margin: 0;
    opacity: 0.9;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-wrapper {
      padding: 16px;
    }

    .hero-container {
      padding: 0 60px;
    }

    .hero-title {
      font-size: 56px;
    }

    .hero-subtitle {
      font-size: 24px;
    }
  }

  @media (max-width: 768px) {
    .hero-wrapper {
      height: auto;
      min-height: 50vh;
      padding: 24px 0;
    }

    .hero-container {
      padding: 0 2rem;
    }

    .hero-title {
      font-size: 32px;
    }

    .hero-subtitle {
      font-size: 20px;
    }

  .hero-description {
    font-size: 16px;
    }
  }

  /* Entradas del blog */
  .blog-entries-section {
    padding: 80px 0 100px 0;
    background: #f0f0f0;
  }

  .blog-entries-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 100px;
    display: grid;
    grid-template-columns: 1fr;
    gap: 32px;
  }

  .blog-entry-card {
    display: block;
    border-radius: 12px;
    background: #fff;
    text-decoration: none;
    color: inherit;
    overflow: hidden;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
    border: 1px solid rgba(0, 0, 0, 0.06);
    transition: box-shadow 0.2s ease, transform 0.2s ease;
  }

  .blog-entry-card:hover {
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    transform: translateY(-2px);
  }

  .blog-entry-image {
    width: 100%;
    height: 500px;
    max-height: 500px;
    overflow: hidden;
    border-radius: 12px 12px 0 0;
  }

  .blog-entry-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .blog-entry-body {
    padding: 28px 32px 32px 32px;
  }

  .blog-entry-date {
    font-family: 'Infra', sans-serif;
    font-size: 14px;
    color: #999;
    display: block;
    margin-bottom: 12px;
  }

  .blog-entry-title {
    font-family: 'Nexa', sans-serif;
    font-size: 22px;
    font-weight: 700;
    color: #1a1a1a;
    line-height: 1.3;
    margin: 0 0 16px 0;
  }

  .blog-entry-excerpt {
    font-family: 'Infra', sans-serif;
    font-size: 16px;
    line-height: 1.6;
    color: #4a4a4a;
    margin: 0 0 24px 0;
  }

  .blog-entry-cta {
    display: flex;
    align-items: flex-start;
    justify-content: flex-end;
    margin-top: 8px;
  }

  .blog-entry-cta .btn {
    pointer-events: none;
  }

  @media (max-width: 768px) {
    .blog-entries-section {
      padding: 48px 0 60px 0;
    }

    .blog-entries-container {
      padding: 0 2rem;
      grid-template-columns: 1fr;
    }

    .blog-entry-body {
      padding: 24px 24px 28px 24px;
    }

    .blog-entry-title {
      font-size: 20px;
    }
  }
</style>
