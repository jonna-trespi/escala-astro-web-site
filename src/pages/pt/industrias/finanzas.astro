---
import Header from "../../../components/Header.astro";
import GoogleAnalytics from "../../../components/GoogleAnalytics.astro";
import Footer from "../../../components/Footer.astro";
import Icon from "../../../components/Icon.astro";
import { getT, localePrefix } from '../../../i18n';
import type { Locale } from '../../../i18n';
import pt from '../../../i18n/pt.json';
import "../../../styles/global.css";

const lang: Locale = 'pt';
const currentPath = Astro.url.pathname;
const dict = pt;
const t = getT(dict);
const basePath = localePrefix(lang);
---

<!doctype html>
<html lang={lang}>
  <head>
    <GoogleAnalytics />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{t('industriaFinanzas.title')}</title>
    <meta name="description" content={t('industriaFinanzas.metaDescription')} />
    <link rel="icon" type="image/png" href="/logos/escala-webclip.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght@400&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <Header lang={lang} currentPath={currentPath} />
    <main>
      <!-- Hero Section -->
      <section class="hero-section">
        <div class="hero-wrapper">
          <div class="hero-banner">
            <div class="hero-container">
              <h1 class="hero-title">
                {t('industriaFinanzas.heroTitle')}<span class="hero-title-bold"
                  >{t('industriaFinanzas.heroTitleBold')}</span
                >{t('industriaFinanzas.heroTitleSuffix')}
              </h1>
              <div class="hero-content">
                <div class="hero-left">
                  <a href="#challenges" class="hero-button">
                    {t('industriaFinanzas.heroButton')}
                    <span class="material-symbols-outlined">north_east</span>
                  </a>
                </div>
                <div class="hero-right">
                  <div class="hero-stat-box">
                    <p class="hero-stat-text">
                      {t('industriaFinanzas.heroStatText')}
                    </p>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Challenges Section -->
      <section class="challenges-section" id="challenges">
        <div class="container">
          <div class="challenges-intro">
            <div class="challenges-intro-content">
              <h2 class="challenges-intro-title">
                {t('industriaFinanzas.challengesTitle')}<span
                  class="challenges-intro-highlight"
                  >{t('industriaFinanzas.challengesTitleHighlight')}</span
                >{t('industriaFinanzas.challengesTitleSuffix')}
              </h2>
              <p class="challenges-intro-text">
                {t('industriaFinanzas.challengesIntroText')}
              </p>
            </div>
            <div class="challenges-intro-image">
              <img
                src="/images/industrias/industrias-escala-imagen-1.jpg"
                alt={t('industriaFinanzas.challengesImageAlt')}
              />
            </div>
          </div>

          <div class="challenges-grid">
            <div class="challenge-card">
              <div class="challenge-icon">
                <img
                  src="/icons/industrias/industrias-icon1.png"
                  alt={t('industriaFinanzas.challenge1Alt')}
                />
              </div>
              <h3 class="challenge-title">
                {t('industriaFinanzas.challenge1Title')}
              </h3>
              <p class="challenge-description">
                {t('industriaFinanzas.challenge1Desc')}
              </p>
            </div>

            <div class="challenge-card">
              <div class="challenge-icon">
                <img
                  src="/icons/industrias/industrias-icon1.png"
                  alt={t('industriaFinanzas.challenge2Alt')}
                />
              </div>
              <h3 class="challenge-title">
                {t('industriaFinanzas.challenge2Title')}
              </h3>
              <p class="challenge-description">
                {t('industriaFinanzas.challenge2Desc')}
              </p>
            </div>

            <div class="challenge-card">
              <div class="challenge-icon">
                <img
                  src="/icons/industrias/industrias-icon1.png"
                  alt={t('industriaFinanzas.challenge3Alt')}
                />
              </div>
              <h3 class="challenge-title">
                {t('industriaFinanzas.challenge3Title')}
              </h3>
              <p class="challenge-description">
                {t('industriaFinanzas.challenge3Desc')}
              </p>
            </div>

            <div class="challenge-card">
              <div class="challenge-icon">
                <img
                  src="/icons/industrias/industrias-icon1.png"
                  alt={t('industriaFinanzas.challenge4Alt')}
                />
              </div>
              <h3 class="challenge-title">
                {t('industriaFinanzas.challenge4Title')}
              </h3>
              <p class="challenge-description">
                {t('industriaFinanzas.challenge4Desc')}
              </p>
            </div>

            <div class="challenge-card">
              <div class="challenge-icon">
                <img
                  src="/icons/industrias/industrias-icon1.png"
                  alt={t('industriaFinanzas.challenge5Alt')}
                />
              </div>
              <h3 class="challenge-title">
                {t('industriaFinanzas.challenge5Title')}
              </h3>
              <p class="challenge-description">
                {t('industriaFinanzas.challenge5Desc')}
              </p>
            </div>

            <div class="challenge-card">
              <div class="challenge-icon">
                <img
                  src="/icons/industrias/industrias-icon1.png"
                  alt={t('industriaFinanzas.challenge6Alt')}
                />
              </div>
              <h3 class="challenge-title">
                {t('industriaFinanzas.challenge6Title')}
              </h3>
              <p class="challenge-description">
                {t('industriaFinanzas.challenge6Desc')}
              </p>
            </div>
          </div>

          <!-- Logos clientes -->
          <div class="clients-logos">
            <img src="/logos/industrias/logo_wester.png" alt="Western Union" />
            <img src="/logos/industrias/logo_allianz.png" alt="Allianz" />
            <img
              src="/logos/industrias/logo_bancolombia.png"
              alt="Bancolombia"
            />
            <img src="/logos/industrias/logo_banesco.png" alt="Banesco" />
            <img src="/logos/industrias/logo_abanca.png" alt="Abanca" />
          </div>
        </div>
      </section>

      <!-- How We Do It & Cloud EMx Section -->
      <section class="escala-methodology-section">
        <div class="container">
          <!-- Title -->
          <div class="methodology-header">
            <h2 class="methodology-title">{t('industriaFinanzas.methodologyTitle')}</h2>
            <p class="methodology-subtitle">
              {t('industriaFinanzas.methodologySubtitle')}
            </p>
          </div>

          <!-- ¿Cómo lo hacemos? -->
          <div class="how-we-do-it">
            <h3 class="how-title">{t('industriaFinanzas.howTitle')}</h3>

            <div class="how-grid">
              <div class="how-card">
                <div class="how-icon">
                  <img
                    src="/icons/industrias/industrias-como-lo-hacemos-icon-1.png"
                    alt={t('industriaFinanzas.how1Alt')}
                  />
                </div>
                <h4 class="how-card-title">
                  {t('industriaFinanzas.how1Title')}
                </h4>
                <p class="how-card-description">
                  {t('industriaFinanzas.how1Desc')}
                </p>
              </div>

              <div class="how-card">
                <div class="how-icon">
                  <img
                    src="/icons/industrias/industrias-como-lo-hacemos-icon-2.png"
                    alt={t('industriaFinanzas.how2Alt')}
                  />
                </div>
                <h4 class="how-card-title">
                  {t('industriaFinanzas.how2Title')}
                </h4>
                <p class="how-card-description">
                  {t('industriaFinanzas.how2Desc')}
                </p>
              </div>

              <div class="how-card">
                <div class="how-icon">
                  <img
                    src="/icons/industrias/industrias-como-lo-hacemos-icon-3.png"
                    alt={t('industriaFinanzas.how3Alt')}
                  />
                </div>
                <h4 class="how-card-title">
                  {t('industriaFinanzas.how3Title')}
                </h4>
                <p class="how-card-description">
                  {t('industriaFinanzas.how3Desc')}
                </p>
              </div>

              <div class="how-card">
                <div class="how-icon">
                  <img
                    src="/icons/industrias/industrias-como-lo-hacemos-icon-4.png"
                    alt={t('industriaFinanzas.how4Alt')}
                  />
                </div>
                <h4 class="how-card-title">
                  {t('industriaFinanzas.how4Title')}
                </h4>
                <p class="how-card-description">
                  {t('industriaFinanzas.how4Desc')}
                </p>
              </div>

              <div class="how-card">
                <div class="how-icon">
                  <img
                    src="/icons/industrias/industrias-como-lo-hacemos-icon-5.png"
                    alt={t('industriaFinanzas.how5Alt')}
                  />
                </div>
                <h4 class="how-card-title">
                  {t('industriaFinanzas.how5Title')}
                </h4>
                <p class="how-card-description">
                  {t('industriaFinanzas.how5Desc')}
                </p>
              </div>

              <div class="how-card">
                <div class="how-icon">
                  <img
                    src="/icons/industrias/industrias-como-lo-hacemos-icon-6.png"
                    alt={t('industriaFinanzas.how6Alt')}
                  />
                </div>
                <h4 class="how-card-title">
                  {t('industriaFinanzas.how6Title')}
                </h4>
                <p class="how-card-description">
                  {t('industriaFinanzas.how6Desc')}
                </p>
              </div>
            </div>
          </div>

          <!-- Cloud EMx Section -->
          <div class="cloud-emx-section">
            <div class="cloud-emx-header">
              <h3 class="cloud-emx-title">
                {t('industriaFinanzas.emxTitle')}
              </h3>
              <p class="cloud-emx-subtitle">
                {t('industriaFinanzas.emxSubtitle')}
              </p>
              <p class="cloud-emx-description">
                {t('industriaFinanzas.emxDescription')}
              </p>
              <p class="cloud-emx-description">
                {t('industriaFinanzas.emxDescription2')}
              </p>
            </div>

            <div class="emx-grid">
              <div class="emx-card">
                <div class="emx-icon">
                  <img
                    src="/icons/industrias/industrias-emx-icon-1.png"
                    alt={t('industriaFinanzas.emx1Alt')}
                  />
                </div>
                <h4 class="emx-card-title">
                  {t('industriaFinanzas.emx1Title')}
                </h4>
              </div>

              <div class="emx-card">
                <div class="emx-icon">
                  <img
                    src="/icons/industrias/industrias-emx-icon-2.png"
                    alt={t('industriaFinanzas.emx2Alt')}
                  />
                </div>
                <h4 class="emx-card-title">
                  {t('industriaFinanzas.emx2Title')}
                </h4>
              </div>

              <div class="emx-card">
                <div class="emx-icon">
                  <img
                    src="/icons/industrias/industrias-emx-icon-3.png"
                    alt={t('industriaFinanzas.emx3Alt')}
                  />
                </div>
                <h4 class="emx-card-title">{t('industriaFinanzas.emx3Title')}</h4>
              </div>

              <div class="emx-card">
                <div class="emx-icon">
                  <img
                    src="/icons/industrias/industrias-emx-icon-4.png"
                    alt={t('industriaFinanzas.emx4Alt')}
                  />
                </div>
                <h4 class="emx-card-title">
                  {t('industriaFinanzas.emx4Title')}
                </h4>
              </div>

              <div class="emx-card">
                <div class="emx-icon">
                  <img
                    src="/icons/industrias/industrias-emx-icon-5.png"
                    alt={t('industriaFinanzas.emx5Alt')}
                  />
                </div>
                <h4 class="emx-card-title">
                  {t('industriaFinanzas.emx5Title')}
                </h4>
              </div>

              <div class="emx-card">
                <div class="emx-icon">
                  <img
                    src="/icons/industrias/industrias-emx-icon-6.png"
                    alt={t('industriaFinanzas.emx6Alt')}
                  />
                </div>
                <h4 class="emx-card-title">
                  {t('industriaFinanzas.emx6Title')}
                </h4>
              </div>
            </div>

            <p class="emx-footer-text">
              {t('industriaFinanzas.emxFooterText')}
            </p>
          </div>
        </div>
      </section>

      <!-- Cloud Done Right Section -->
      <section class="cloud-done-right-section">
        <div class="container">
          <div class="cdr-grid">
            <!-- Large Card -->
            <div class="cdr-card cdr-card-large">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdrTitle')}</h3>
              <h4 class="cdr-card-subtitle">{t('industriaFinanzas.cdrSubtitle')}</h4>
              <p class="cdr-card-text">{t('industriaFinanzas.cdrText1')}</p>
              <p class="cdr-card-text">{t('industriaFinanzas.cdrText2')}</p>
            </div>

            <!-- Small Cards - Row 1 -->
            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr1Title')}</h3>
              <p class="cdr-card-text">
                {t('industriaFinanzas.cdr1Text')}
              </p>
            </div>

            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr2Title')}</h3>
              <p class="cdr-card-text">
                {t('industriaFinanzas.cdr2Text')}
              </p>
            </div>

            <!-- Small Cards - Row 2 -->
            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr3Title')}</h3>
              <p class="cdr-card-text">{t('industriaFinanzas.cdr3Text')}</p>
            </div>

            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr4Title')}</h3>
              <p class="cdr-card-text">{t('industriaFinanzas.cdr4Text')}</p>
            </div>

            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr5Title')}</h3>
              <p class="cdr-card-text">{t('industriaFinanzas.cdr5Text')}</p>
            </div>

            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr6Title')}</h3>
              <p class="cdr-card-text">{t('industriaFinanzas.cdr6Text')}</p>
            </div>

            <div class="cdr-card cdr-card-small">
              <h3 class="cdr-card-title">{t('industriaFinanzas.cdr7Title')}</h3>
              <p class="cdr-card-text">{t('industriaFinanzas.cdr7Text')}</p>
            </div>
          </div>
        </div>
      </section>

      <!-- 7R Section -->
      <section class="seven-r-section">
        <div class="container">
          <div class="seven-r-header">
            <div class="seven-r-header-left">
              <h2 class="seven-r-title" set:html={t('industriaFinanzas.sevenRTitle')}></h2>
            </div>
            <div class="seven-r-header-right">
              <p class="seven-r-description">{t('industriaFinanzas.sevenRDescription')}</p>
            </div>
          </div>

          <div class="seven-r-carousel">
            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR1Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR1Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">1</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-1.jpg');"></div>
            </div>

            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR2Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR2Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">2</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-2.jpg');"></div>
            </div>

            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR3Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR3Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">3</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-3.jpg');"></div>
            </div>

            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR4Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR4Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">4</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-4.jpg');"></div>
            </div>

            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR5Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR5Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">5</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-5.jpg');"></div>
            </div>

            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR6Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR6Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">6</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-6.jpg');"></div>
            </div>

            <div class="seven-r-card">
              <div class="seven-r-card-content">
                <h3 class="seven-r-card-title">{t('industriaFinanzas.sevenR7Title')}</h3>
                <p class="seven-r-card-subtitle">{t('industriaFinanzas.sevenR7Subtitle')}</p>
              </div>
              <div class="seven-r-card-number">7</div>
              <div class="seven-r-card-bg" style="background-image: url('/images/industrias/industrias-escala-7r-7.jpg');"></div>
            </div>
          </div>
        </div>
      </section>

      <!-- Accelerator Section -->
      <section class="accelerator-section">
        <div class="accelerator-container">
          <!-- Cases Section -->
          <div class="cases-section-wrapper">
            <div class="cases-header">
              <p class="cases-pretitle">{t('industriaFinanzas.casesPretitle')}</p>
              <h2 class="cases-title">{t('industriaFinanzas.casesTitle')}</h2>
            </div>

            <div class="cases-video-grid">
              <button 
                class="case-video-card"
                data-video-id="Ag5PBRJAv1Q"
                data-video-title="Allianz"
              >
                <div class="video-thumbnail" style="background-image: url('https://img.youtube.com/vi/Ag5PBRJAv1Q/hqdefault.jpg');" role="img" aria-label={t('industriaFinanzas.case1Alt')}></div>
                <div class="video-overlay">
                  <div class="play-button-circle">
                    <Icon name="play_arrow" size={24} />
                  </div>
                </div>
                <div class="video-info">
                  <img src="/logos/industrias/casos/logo-allianz.png" alt={t('industriaFinanzas.case1Alt')} class="company-logo" />
                  <p class="video-description">
                    {t('industriaFinanzas.case1Description')}
                  </p>
                </div>
              </button>

              <button 
                class="case-video-card"
                data-video-id="X2Bvtr_RBLU"
                data-video-title="Mercantil Andina"
              >
                <div class="video-thumbnail" style="background-image: url('https://img.youtube.com/vi/X2Bvtr_RBLU/hqdefault.jpg');" role="img" aria-label={t('industriaFinanzas.case2Alt')}></div>
                <div class="video-overlay">
                  <div class="play-button-circle">
                    <Icon name="play_arrow" size={24} />
                  </div>
                </div>
                <div class="video-info">
                  <img src="/logos/industrias/casos/logo-mercantil.png" alt={t('industriaFinanzas.case2Alt')} class="company-logo" />
                  <p class="video-description">
                    {t('industriaFinanzas.case2Description')}
                  </p>
                </div>
              </button>

              <button 
                class="case-video-card"
                data-video-id="9Z2i7pvt-UU"
                data-video-title="Grupo San Cristóbal"
              >
                <div class="video-thumbnail" style="background-image: url('https://img.youtube.com/vi/9Z2i7pvt-UU/hqdefault.jpg');" role="img" aria-label={t('industriaFinanzas.case3Alt')}></div>
                <div class="video-overlay">
                  <div class="play-button-circle">
                    <Icon name="play_arrow" size={24} />
                  </div>
                </div>
                <div class="video-info">
                  <img src="/logos/industrias/casos/logo-gsc.png" alt={t('industriaFinanzas.case3Alt')} class="company-logo" />
                  <p class="video-description">
                    {t('industriaFinanzas.case3Description')}
                  </p>
                </div>
              </button>
            </div>
          </div>

          <!-- CTA Card -->
          <div class="cta-card">
            <div class="cta-card-content">
              <div class="cta-card-text">
                <h2 class="cta-card-title">
                  {t('industriaFinanzas.ctaTitle')}
                </h2>
                <p class="cta-card-description">
                  {t('industriaFinanzas.ctaDescription')}
                </p>
              </div>
              <a href={`${basePath}/contacto`} class="btn btn-primary">
                {t('industriaFinanzas.ctaButton')}
                <span class="material-symbols-outlined">north_east</span>
              </a>
            </div>
          </div>

          <!-- Video Modal -->
          <div class="video-modal" id="videoModalFinanzas">
            <div class="modal-overlay"></div>
            <div class="modal-content">
              <button class="modal-close" id="modalCloseFinanzas">
                <span class="material-symbols-outlined">close</span>
              </button>
              <div class="modal-video">
                <iframe
                  id="modalIframeFinanzas"
                  src=""
                  frameborder="0"
                  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                  allowfullscreen
                ></iframe>
              </div>
            </div>
          </div>

          <!-- Footer Embedded -->
          <!-- Footer Component -->
          <Footer lang={lang} />
        </div>
      </section>
    </main>

    <script>
      // Drag to scroll functionality for 7R carousel
      document.addEventListener('DOMContentLoaded', () => {
        const carousel = document.querySelector('.seven-r-carousel') as HTMLElement | null;
        if (!carousel) return;

        let isDown = false;
        let startX: number = 0;
        let scrollLeft: number = 0;

        carousel.addEventListener('mousedown', (e: MouseEvent) => {
          isDown = true;
          carousel.classList.add('grabbing');
          startX = e.pageX - carousel.offsetLeft;
          scrollLeft = carousel.scrollLeft;
          e.preventDefault();
        });

        carousel.addEventListener('mouseleave', () => {
          isDown = false;
          carousel.classList.remove('grabbing');
        });

        carousel.addEventListener('mouseup', () => {
          isDown = false;
          carousel.classList.remove('grabbing');
        });

        carousel.addEventListener('mousemove', (e: MouseEvent) => {
          if (!isDown) return;
          e.preventDefault();
          const x = e.pageX - carousel.offsetLeft;
          const walk = (x - startX) * 2; // Scroll speed multiplier
          carousel.scrollLeft = scrollLeft - walk;
        });

        // Prevent clicks when dragging
        let isDragging = false;
        carousel.addEventListener('mousedown', () => {
          isDragging = false;
        });

        carousel.addEventListener('mousemove', () => {
          isDragging = true;
        });

        carousel.addEventListener('click', (e: MouseEvent) => {
          if (isDragging) {
            e.preventDefault();
            e.stopPropagation();
          }
        }, true);
      });

      // Video Modal functionality for Finanzas section
      const videoButtonsFinanzas = document.querySelectorAll('.case-video-card');
      const videoModalFinanzas = document.getElementById('videoModalFinanzas');
      const modalCloseFinanzas = document.getElementById('modalCloseFinanzas');
      const modalOverlayFinanzas = videoModalFinanzas?.querySelector('.modal-overlay');
      const modalIframeFinanzas = document.getElementById('modalIframeFinanzas') as HTMLIFrameElement | null;

      videoButtonsFinanzas.forEach(button => {
        button.addEventListener('click', () => {
          const videoId = button.getAttribute('data-video-id');
          
          if (videoId && modalIframeFinanzas && videoModalFinanzas) {
            // Set video URL with autoplay
            modalIframeFinanzas.src = `https://www.youtube.com/embed/${videoId}?autoplay=1&controls=1&modestbranding=1&rel=0`;
            
            // Show modal
            videoModalFinanzas.classList.add('active');
            document.body.style.overflow = 'hidden';
          }
        });
      });

      // Close modal
      function closeModalFinanzas() {
        if (videoModalFinanzas && modalIframeFinanzas) {
          videoModalFinanzas.classList.remove('active');
          modalIframeFinanzas.src = '';
          document.body.style.overflow = '';
        }
      }

      modalCloseFinanzas?.addEventListener('click', closeModalFinanzas);
      modalOverlayFinanzas?.addEventListener('click', closeModalFinanzas);

      // Close with Escape key
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && videoModalFinanzas?.classList.contains('active')) {
          closeModalFinanzas();
        }
      });
    </script>
  </body>
</html>


<style>
  html {
    scroll-behavior: smooth;
  }

  /* Hero Section */
  .hero-section {
    position: relative;
    width: 100%;
    background-color: #0c0c0c;
  }

  .hero-wrapper {
    padding: 24px;
    height: 90vh;
    min-height: 700px;
  }

  .hero-banner {
    position: relative;
    width: 100%;
    height: 100%;
    border-radius: 24px;
    overflow: hidden;
    background-image: url("/images/industrias/industrias-hero.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    display: flex;
    align-items: center;
    padding-top: 100px;
  }

  .hero-container {
    position: relative;
    z-index: 1;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 100px;
    width: 100%;
    display: flex;
    flex-direction: column;
    min-height: calc(100vh - 100px);
    justify-content: center;
  }

  .hero-title {
    color: var(--neutral-100);
    font-size: 64px;
    font-weight: 500;
    line-height: 72px;
    margin-bottom: 64px;
    max-width: 70%;
  }

  .hero-title-bold {
    font-weight: 500;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-left {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
    align-items: flex-start;
    justify-content: center;
    max-width: 100%;
  }

  .hero-right {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: flex-start;
    justify-content: center;
  }

  .hero-stat-box {
    position: relative;
    padding: 0;
    margin-bottom: 0;
    max-width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .hero-stat-box::before {
    display: none;
  }

  .hero-stat-text {
    color: var(--neutral-100);
    font-size: 18px;
    font-weight: 400;
    line-height: 1.6;
    margin: 0;
    opacity: 0.9;
  }

  .hero-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    background: #00c19f;
    color: var(--neutral-800);
    font-size: 16px;
    font-weight: 500;
    text-decoration: none;
    border-radius: 4px;
    transition: all 0.3s ease;
    white-space: nowrap;
    border: none;
    cursor: pointer;
  }

  .hero-button:hover {
    background: #00a585;
    transform: translateY(-2px);
  }

  .hero-button .material-symbols-outlined {
    font-size: 20px;
    width: 20px;
    height: 20px;
  }

  /* Common Section Styles */
  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 100px;
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-title {
    color: var(--neutral-800);
    font-size: 48px;
    font-weight: 500;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    color: var(--neutral-600);
    font-size: 18px;
    font-weight: 400;
    max-width: 800px;
    margin: 0 auto;
  }

  /* Challenges Section */
  .challenges-section {
    padding: 100px 0;
    background-color: #f4f4f4;
  }

  .challenges-intro {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
    margin-bottom: 4rem;
  }

  .challenges-intro-content {
    max-width: 580px;
  }

  .challenges-intro-title {
    color: #1a1a1a;
    font-size: 48px;
    font-family: "Nexa", sans-serif;
    font-weight: 600;
    font-style: normal;
    line-height: 1;
    margin-bottom: 1.5rem;
  }

  .challenges-intro-highlight {
    color: #00725D;
    font-weight: 500;
    font-style: normal;
  }

  .challenges-intro-text {
    color: #1a1a1a;
    font-size: 18px;
    font-weight: 400;
    line-height: 1.6;
    margin: 0;
  }

  .challenges-intro-image {
    position: relative;
    width: 100%;
    max-width: 430px;
    height: 400px;
    border-radius: 20px;
    overflow: hidden;
    margin-left: auto;
  }

  .challenges-intro-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .challenges-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-bottom: 4rem;
  }

  .challenge-card {
    background: #e6e6e6;
    border-radius: 20px;
    padding: 2rem;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
    cursor: pointer;
    min-height: 200px;
    display: flex;
    flex-direction: column;
  }

  .challenge-card:hover {
    background: #ebfffb;
  }

  .challenge-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    flex-shrink: 0;
  }

  .challenge-icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .challenge-title {
    color: var(--neutral-800);
    font-size: 18px;
    font-weight: 600;
    line-height: 1.4;
    margin-bottom: 0;
    transition: margin-bottom 0.4s ease;
  }

  .challenge-card:hover .challenge-title {
    margin-bottom: 1rem;
  }

  .challenge-description {
    color: var(--neutral-600);
    font-size: 14px;
    font-weight: 400;
    line-height: 1.6;
    margin: 0;
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .challenge-card:hover .challenge-description {
    max-height: 200px;
    opacity: 1;
    transform: translateY(0);
  }

  /* Logos clientes */
  .clients-logos {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 3rem;
    flex-wrap: wrap;
    padding-top: 2rem;
  }

  .clients-logos img {
    height: 40px;
    width: auto;
    object-fit: contain;
    opacity: 0.7;
    transition: opacity 0.3s ease;
    flex-shrink: 0;
  }

  .clients-logos img:hover {
    opacity: 1;
  }

  /* Escala Methodology Section */
  .escala-methodology-section {
    position: relative;
    padding: 100px 0;
    background-image: url("/images/industrias/industrias-escala-bg.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed;
  }

  .escala-methodology-section .container {
    position: relative;
    z-index: 2;
  }

  .methodology-header {
    text-align: center;
    margin-bottom: 116px;
    max-width: 760px;
    margin-left: auto;
    margin-right: auto;
  }

  .methodology-title {
    color: var(--neutral-100);
    font-size: 44px;
    font-family: "Nexa", sans-serif;
    font-weight: 600;
    line-height: 48px;
    margin-bottom: 8px;
  }

  .methodology-subtitle {
    color: var(--neutral-100);
    font-size: 20px;
    font-weight: 400;
    line-height: 28px;
    margin: 0;
  }

  /* How We Do It Section */
  .how-we-do-it {
    margin-bottom: 8rem;
  }

  .how-title {
    color: var(--neutral-100);
    font-size: 32px;
    font-weight: 600;
    line-height: 32px;
    text-align: center;
    margin-bottom: 24px;
  }

  .how-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
  }

  .how-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .how-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }

  .how-icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .how-card-title {
    color: var(--neutral-100);
    font-size: 20px;
    font-weight: 600;
    line-height: 124%;
    margin-bottom: 0.75rem;
  }

  .how-card-description {
    color: var(--neutral-100);
    font-size: 16px;
    font-weight: 400;
    line-height: 20px;
    margin: 0;
  }

  /* Cloud EMx Section */
  .cloud-emx-section {
    position: relative;
    border-radius: 20px;
    padding: 9rem 8rem;
    background: radial-gradient(
      circle,
      rgba(1, 66, 79, 0.25) 0%,
      rgba(2, 68, 81, 0.25) 50%,
      rgba(0, 48, 58, 0.25) 100%
    );
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.5);
  }

  .cloud-emx-header {
    text-align: center;
    margin-bottom: 80px;
    position: relative;
    z-index: 1;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
  }

  .cloud-emx-title {
    color: var(--neutral-100);
    font-size: 44px;
    font-family: "Nexa", sans-serif;
    font-weight: 700;
    line-height: 48px;
    margin-bottom: 1rem;
  }

  .cloud-emx-subtitle {
    color: var(--neutral-100);
    font-size: 24px;
    font-weight: 500;
    margin-bottom: 56px;
  }

  .cloud-emx-description {
    color: var(--neutral-100);
    font-size: 20px;
    font-weight: 400;
    line-height: 28px;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
  }

  .emx-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    margin-bottom: 80px;
    position: relative;
    z-index: 1;
  }

  .emx-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 2.5rem 2rem;
    border-right: 1px solid rgba(255, 255, 255, 0.2);
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
  }

  .emx-card:nth-child(3n) {
    border-right: none;
  }

  .emx-card:nth-child(4),
  .emx-card:nth-child(5),
  .emx-card:nth-child(6) {
    border-bottom: none;
  }

  .emx-icon {
    width: 63px;
    height: 63px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }

  .emx-icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .emx-card-title {
    color: var(--neutral-100);
    font-size: 16px;
    font-weight: 600;
    line-height: 1.4;
    margin: 0;
  }

  .emx-footer-text {
    color: var(--neutral-100);
    font-size: 14px;
    font-weight: 400;
    line-height: 1.6;
    text-align: center;
    margin: 0;
    opacity: 0.8;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 1;
  }

  /* Cloud Done Right Section */
  .cloud-done-right-section {
    padding: 100px 0;
    background-color: #FAFAFA;
  }

  .cdr-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .cdr-card {
    border-radius: 16px;
    padding: 2.5rem;
    display: flex;
    flex-direction: column;
    transition: background-color 0.3s ease;
    cursor: pointer;
  }

  .cdr-card:hover {
    background-color: #041E42;
  }

  .cdr-card-large {
    grid-column: 1 / 2;
    grid-row: 1 / 3;
    background-color: #020E1E;
  }

  .cdr-card-small {
    background-color: #020E1E;
  }

  .cdr-card-title {
    color: #00C19F;
    font-size: 24px;
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 1rem;
  }

  .cdr-card-subtitle {
    color: var(--neutral-100);
    font-size: 24px;
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 1.5rem;
  }

  .cdr-card-text {
    color: var(--neutral-100);
    font-size: 16px;
    font-weight: 400;
    line-height: 1.6;
    margin: 0 0 1rem 0;
    opacity: 0.9;
  }

  .cdr-card-text:last-child {
    margin-bottom: 0;
  }

  /* 7R Section */
  .seven-r-section {
    padding: 100px 0;
    background-color: #FAFAFA;
  }

  .seven-r-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: flex-end;
    margin-bottom: 4rem;
  }

  .seven-r-title {
    color: var(--neutral-800);
    font-size: 44px;
    font-family: 'Nexa', sans-serif;
    font-weight: 700;
    line-height: 48px;
    margin: 0;
  }

  .seven-r-description {
    color: var(--neutral-800);
    font-size: 16px;
    font-weight: 400;
    line-height: 20px;
    margin: 0;
    text-align: right;
  }

  .seven-r-carousel {
    display: flex;
    gap: 1.5rem;
    overflow-x: auto;
    overflow-y: hidden;
    scroll-snap-type: x mandatory;
    padding-bottom: 1rem;
    cursor: grab;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
    scrollbar-color: rgba(0, 0, 0, 0.3) transparent;
  }

  .seven-r-carousel::-webkit-scrollbar {
    height: 8px;
  }

  .seven-r-carousel::-webkit-scrollbar-track {
    background: transparent;
    margin: 0 100px;
  }

  .seven-r-carousel::-webkit-scrollbar-thumb {
    background: rgba(0, 0, 0, 0.3);
    border-radius: 4px;
  }

  .seven-r-carousel::-webkit-scrollbar-thumb:hover {
    background: rgba(0, 0, 0, 0.5);
  }

  .seven-r-carousel.grabbing {
    cursor: grabbing;
    scroll-snap-type: none;
  }

  .seven-r-card {
    position: relative;
    min-width: 760px;
    max-width: 760px;
    min-height: 500px;
    height: 500px;
    border-radius: 24px;
    overflow: hidden;
    padding: 40px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    scroll-snap-align: start;
    flex-shrink: 0;
    user-select: none;
  }

  .seven-r-card-bg {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    z-index: 0;
  }

  .seven-r-card-number {
    position: relative;
    z-index: 2;
    color: var(--neutral-100);
    font-size: 44px;
    font-weight: 700;
    line-height: 1;
    margin: 0;
    align-self: flex-start;
  }

  .seven-r-card-title {
    position: relative;
    z-index: 2;
    color: var(--neutral-100);
    font-size: 44px;
    font-weight: 700;
    line-height: 1.2;
    margin: 0 0 0.75rem 0;
  }

  .seven-r-card-subtitle {
    position: relative;
    z-index: 2;
    color: var(--neutral-100);
    font-size: 22px;
    font-weight: 400;
    line-height: 1.4;
    margin: 0;
  }

  .seven-r-card-content {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    max-width: 300px;
  }

  /* Accelerator Section */
  .accelerator-section {
    width: 100%;
    background-image: url('/images/soluciones_footer.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: 100px 0 80px 0;
  }

  .accelerator-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 100px;
  }

  /* Cases Section */
  .cases-section-wrapper {
    margin-bottom: 5rem;
  }

  .cases-header {
    text-align: left;
    margin-bottom: 3rem;
  }

  .cases-pretitle {
    color: var(--neutral-100);
    font-size: 16px;
    font-weight: 400;
    margin-bottom: 0.5rem;
    opacity: 0.9;
  }

  .cases-title {
    color: var(--neutral-100);
    font-size: 32px;
    font-weight: 600;
    margin: 0;
    line-height: 1.2;
  }

  .cases-video-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: minmax(450px, 1fr);
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .case-video-card {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    cursor: pointer;
    border: none;
    background: var(--neutral-800);
    padding: 0;
    transition: all 0.3s ease;
    display: block;
    width: 100%;
    height: 100%;
    min-height: 450px;
  }

  .case-video-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
  }

  .video-thumbnail {
    position: absolute;
    top: -80px;
    left: 0;
    right: 0;
    bottom: -80px;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .video-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.35);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: background 0.3s ease;
  }

  .case-video-card:hover .video-overlay {
    background: rgba(0, 0, 0, 0.45);
  }

  .play-button-circle {
    width: 48px;
    height: 48px;
    background: rgba(255, 255, 255, 0.9);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--neutral-800);
    transition: all 0.3s ease;
  }

  .play-button-circle .material-symbols-outlined {
    font-size: 36px;
  }

  .case-video-card:hover .play-button-circle {
    background: var(--neutral-100);
    transform: scale(1.15);
  }

  .video-info {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1.5rem;
    color: var(--neutral-100);
    z-index: 2;
    text-align: left;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .company-logo {
    height: 40px;
    width: auto;
    max-width: 140px;
    margin-bottom: 0.75rem;
    object-fit: contain;
    filter: brightness(0) invert(1);
  }

  .video-description {
    font-family: 'Infra', sans-serif;
    font-size: 16px;
    line-height: 20px;
    margin: 0;
    opacity: 0.95;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-align: left;
    max-height: 60px;
    transition: max-height 0.4s ease, transform 0.4s ease;
  }

  .case-video-card:hover .video-description {
    -webkit-line-clamp: unset;
    max-height: 500px;
    transform: translateY(-6px);
  }

  /* CTA Card */
  .cta-card {
    margin-bottom: 5rem;
    max-width: 1100px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-card-content {
    background: radial-gradient(
      circle,
      rgba(1, 66, 79, 0.25) 0%,
      rgba(2, 68, 81, 0.25) 50%,
      rgba(0, 48, 58, 0.25) 100%
    );
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 24px;
    padding: 44px 96px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 5rem;
    position: relative;
    overflow: hidden;
  }

  .cta-card-content .cta-card-text,
  .cta-card-content .btn {
    position: relative;
    z-index: 1;
  }

  .cta-card-text {
    flex: 1;
    text-align: left;
  }

  .cta-card-title {
    color: var(--neutral-100);
    font-size: 44px;
    font-weight: 700;
    margin-bottom: 2.5rem;
    line-height: 1.2;
  }

  .cta-card-description {
    color: var(--neutral-300);
    font-size: 16px;
    font-weight: 400;
    margin-bottom: 0;
    line-height: 20px;
    width: 80%;
    max-width: 100%;
  }

  .cta-card-content .btn {
    flex-shrink: 0;
    align-self: flex-end;
  }

  /* Video Modal */
  .video-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 9999;
  }

  .video-modal.active {
    display: block;
  }

  .modal-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    cursor: pointer;
  }

  .modal-content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 90%;
    max-width: 1200px;
    z-index: 10000;
  }

  .modal-close {
    position: absolute;
    top: -50px;
    right: 0;
    width: 50px;
    height: 50px;
    background: rgba(255, 255, 255, 0.1);
    border: 2px solid var(--neutral-100);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--neutral-100);
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 0;
  }

  .modal-close:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: scale(1.1);
  }

  .modal-close .material-symbols-outlined {
    font-size: 32px;
  }

  .modal-video {
    position: relative;
    width: 100%;
    padding-top: 56.25%;
    background: var(--neutral-800);
    border-radius: 8px;
    overflow: hidden;
  }

  .modal-video iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
  }

  /* Footer Embedded */
  .footer-embedded {
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    background-image: url("/images/footer.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .footer-content {
    position: relative;
    z-index: 1;
    padding: 60px;
    display: flex;
    flex-direction: column;
    gap: 2rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .footer-main-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr) auto;
    gap: 2rem;
    align-items: start;
    width: 100%;
  }

  .footer-logo-container {
    display: flex;
    justify-content: flex-end;
    align-items: flex-start;
  }

  .footer-logo {
    width: auto;
    max-width: 200px;
    height: auto;
  }

  .footer-column {
    display: flex;
    flex-direction: column;
  }

  .footer-column-title {
    color: var(--neutral-100);
    font-size: 14px;
    font-weight: 700;
    margin-bottom: 1rem;
    margin-top: 0;
  }

  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .footer-links a {
    color: var(--neutral-100);
    font-size: 13px;
    font-weight: 400;
    text-decoration: none;
    opacity: 0.8;
    transition: all 0.3s ease;
  }

  .footer-links a:hover {
    opacity: 1;
    color: var(--light-green-400);
  }

  .footer-social {
    display: flex;
    gap: 0.75rem;
    justify-content: flex-end;
    align-items: center;
  }

  .social-link {
    width: 36px;
    height: 36px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--neutral-100);
    transition: all 0.3s ease;
    text-decoration: none;
  }

  .social-link:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: var(--light-green-400);
    color: var(--light-green-400);
    transform: translateY(-2px);
  }

  .footer-bottom {
    position: relative;
    z-index: 1;
    padding: 1.5rem 60px;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    text-align: center;
    align-items: center;
  }

  .footer-legal {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.25rem;
    font-size: 11px;
    line-height: 1.4;
  }

  .footer-legal a {
    color: var(--neutral-100);
    text-decoration: none;
    opacity: 0.6;
    transition: opacity 0.3s ease;
    white-space: nowrap;
  }

  .footer-legal a:hover {
    opacity: 1;
  }

  .footer-legal a:not(:last-child)::after {
    content: "•";
    margin-left: 0.25rem;
    opacity: 0.5;
  }

  .footer-copyright {
    font-size: 11px;
    color: var(--neutral-100);
    opacity: 0.6;
    margin: 0;
    line-height: 1.4;
  }

  /* Solutions Section */
  .solutions-section {
    padding: 100px 0;
    background-color: white;
  }

  .solutions-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }

  .solution-card {
    background: white;
    border-radius: 12px;
    padding: 2rem;
    border: 1px solid #e5e7eb;
    transition: all 0.3s ease;
  }

  .solution-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
    border-color: #00c19f;
  }

  .solution-icon {
    width: 48px;
    height: 48px;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .solution-icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .solution-title {
    color: var(--neutral-800);
    font-size: 18px;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .solution-description {
    color: var(--neutral-600);
    font-size: 14px;
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .solution-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #00c19f;
    font-size: 14px;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .solution-link:hover {
    gap: 0.75rem;
  }

  .solution-link .material-symbols-outlined {
    font-size: 18px;
  }

  /* Use Cases Section */
  .use-cases-section {
    padding: 100px 0;
    background-color: #f8f9fa;
  }

  .use-cases-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .use-case-card {
    display: grid;
    grid-template-columns: 45% 55%;
    gap: 3rem;
    background: white;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
  }

  .use-case-card:nth-child(even) {
    grid-template-columns: 55% 45%;
  }

  .use-case-card:nth-child(even) .use-case-image {
    order: 2;
  }

  .use-case-image {
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  .use-case-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .use-case-content {
    padding: 3rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .use-case-title {
    color: var(--neutral-800);
    font-size: 28px;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .use-case-description {
    color: var(--neutral-600);
    font-size: 16px;
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .use-case-benefits {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .use-case-benefits li {
    color: var(--neutral-700);
    font-size: 15px;
    font-weight: 400;
    padding-left: 1.5rem;
    margin-bottom: 0.75rem;
    position: relative;
  }

  .use-case-benefits li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: #00c19f;
    font-weight: 700;
  }

  /* CTA Section */
  .cta-section {
    padding: 100px 0;
    background-color: white;
  }

  .cta-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .cta-title {
    color: white;
    font-size: 36px;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .cta-description {
    color: rgba(255, 255, 255, 0.9);
    font-size: 18px;
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: 2rem;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    background: #00c19f;
    color: var(--neutral-800);
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.3s ease;
  }

  .cta-button:hover {
    background: #00a585;
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(0, 193, 159, 0.3);
  }

  .cta-button .material-symbols-outlined {
    font-size: 20px;
  }

  /* Contact Section */
  .contact-section {
    width: 100%;
    background-color: #0c0c0c;
    padding: 24px;
    padding-top: 100px;
  }

  .container-contact {
    max-width: 1400px;
    margin: 0 auto;
  }

  /* Footer Embedded */
  .footer-embedded {
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    background-image: url("/images/footer.jpg");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
  }

  .footer-content {
    position: relative;
    z-index: 1;
    padding: 60px 80px;
  }

  .footer-main-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr) auto;
    gap: 2rem;
    align-items: start;
    width: 100%;
  }

  .footer-logo-container {
    display: flex;
    justify-content: flex-end;
    align-items: flex-start;
  }

  .footer-logo {
    width: auto;
    max-width: 200px;
    height: auto;
  }

  .footer-column {
    display: flex;
    flex-direction: column;
  }

  .footer-column-title {
    color: var(--neutral-100);
    font-size: 14px;
    font-weight: 700;
    margin-bottom: 1rem;
    margin-top: 0;
  }

  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .footer-links a {
    color: var(--neutral-100);
    font-size: 13px;
    font-weight: 400;
    text-decoration: none;
    opacity: 0.8;
    transition: all 0.3s ease;
  }

  .footer-links a:hover {
    opacity: 1;
    color: var(--light-green-400);
  }

  .footer-social {
    display: flex;
    gap: 0.75rem;
    justify-content: flex-end;
    align-items: center;
  }

  .social-link {
    width: 36px;
    height: 36px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--neutral-100);
    transition: all 0.3s ease;
    text-decoration: none;
  }

  .social-link:hover {
    background: rgba(255, 255, 255, 0.2);
    border-color: var(--light-green-400);
    color: var(--light-green-400);
    transform: translateY(-2px);
  }

  .footer-bottom {
    position: relative;
    z-index: 1;
    padding: 30px 80px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .footer-legal {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem;
    justify-content: center;
  }

  .footer-legal a {
    color: var(--neutral-100);
    font-size: 12px;
    font-weight: 400;
    text-decoration: none;
    opacity: 0.7;
    transition: all 0.3s ease;
  }

  .footer-legal a:hover {
    opacity: 1;
    color: var(--light-green-400);
  }

  .footer-copyright {
    color: var(--neutral-100);
    font-size: 12px;
    font-weight: 400;
    text-align: center;
    margin: 0;
    opacity: 0.6;
  }

  /* Responsive Design */
  @media (max-width: 1024px) {
    .container {
      padding: 0 3rem;
    }

    .hero-container {
      padding: 0 3rem;
    }

    .challenges-intro {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .challenges-intro-title {
      font-size: 36px;
    }

    .challenges-intro-text {
      font-size: 16px;
    }

    .challenges-intro-image {
      height: 300px;
    }

    .challenges-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }

    .challenge-card {
      padding: 1.5rem;
      min-height: 180px;
    }

    .clients-logos {
      gap: 2rem;
      padding-top: 2rem;
    }

    .clients-logos img {
      height: 36px;
    }

    .methodology-header {
      margin-bottom: 4rem;
    }

    .methodology-title {
      font-size: 36px;
    }

    .methodology-subtitle {
      font-size: 16px;
    }

    .how-we-do-it {
      margin-bottom: 4rem;
    }

    .how-title {
      font-size: 24px;
      margin-bottom: 2rem;
    }

    .how-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
    }

    .how-icon {
      width: 56px;
      height: 56px;
    }

    .how-card-title {
      font-size: 16px;
    }

    .cloud-emx-section {
      padding: 2.5rem;
    }

    .cloud-emx-title {
      font-size: 32px;
    }

    .cloud-emx-subtitle {
      font-size: 20px;
    }

    .cloud-emx-description {
      font-size: 15px;
    }

    .emx-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .emx-card {
      padding: 2rem 1.5rem;
    }

    .emx-card:nth-child(3n) {
      border-right: 1px solid rgba(255, 255, 255, 0.2);
    }

    .emx-card:nth-child(2n) {
      border-right: none;
    }

    .emx-card:nth-child(4),
    .emx-card:nth-child(5),
    .emx-card:nth-child(6) {
      border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

    .emx-card:nth-child(5),
    .emx-card:nth-child(6) {
      border-bottom: none;
    }

    .emx-icon {
      width: 56px;
      height: 56px;
    }

    .emx-card-title {
      font-size: 15px;
    }

    .emx-footer-text {
      font-size: 13px;
    }

    .cdr-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .cdr-card-large {
      grid-column: 1 / -1;
      grid-row: auto;
    }

    .cdr-card {
      padding: 2rem;
    }

    .cdr-card-title {
      font-size: 20px;
    }

    .cdr-card-subtitle {
      font-size: 20px;
    }

    .cdr-card-text {
      font-size: 15px;
    }

    .seven-r-header {
      grid-template-columns: 1fr;
      gap: 2rem;
      margin-bottom: 3rem;
    }

    .seven-r-title {
      font-size: 36px;
    }

    .seven-r-description {
      font-size: 16px;
    }

    .seven-r-carousel {
      gap: 1rem;
    }

    .seven-r-card {
      min-width: 600px;
      max-width: 600px;
      min-height: 450px;
      height: 450px;
      padding: 40px;
    }

    .seven-r-card-number {
      font-size: 36px;
    }

    .seven-r-card-title {
      font-size: 36px;
    }

    .seven-r-card-subtitle {
      font-size: 18px;
    }

    .accelerator-container {
      padding: 0 60px;
    }

    .cases-title {
      font-size: 38px;
    }

    .cases-video-grid {
      grid-template-columns: 1fr;
    }

    .case-video-card {
      min-height: 350px;
    }

    .cta-card-content {
      padding: 2.5rem 2rem;
      flex-direction: column;
      text-align: center;
      gap: 1.5rem;
    }

    .cta-card-text {
      text-align: center;
    }

    .cta-card-title {
      font-size: 32px;
    }

    .cta-card-description {
      font-size: 15px;
    }

    .footer-content {
      padding: 50px;
    }

    .footer-main-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .footer-logo-container {
      justify-content: center;
      order: -1;
    }

    .footer-column {
      text-align: center;
    }

    .footer-social {
      justify-content: center;
    }

    .solutions-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .use-case-card {
      grid-template-columns: 1fr;
    }

    .use-case-card:nth-child(even) {
      grid-template-columns: 1fr;
    }

    .use-case-card:nth-child(even) .use-case-image {
      order: 0;
    }
  }

  @media (max-width: 768px) {
    /* Evitar que el contenido se salga de la margen */
    body, main {
      overflow-x: hidden;
    }
    .container, .accelerator-container {
      padding-left: 1.5rem;
      padding-right: 1.5rem;
      max-width: 100%;
      box-sizing: border-box;
    }
    img {
      max-width: 100%;
      height: auto;
    }
    * {
      box-sizing: border-box;
    }

    /* Todos los textos con line-height 1.2 en mobile */
    .hero-title,
    .hero-stat-text,
    .challenges-intro-title,
    .challenges-intro-text,
    .challenge-title,
    .challenge-description,
    .methodology-title,
    .methodology-subtitle,
    .how-title,
    .how-card-title,
    .how-card-description,
    .cloud-emx-title,
    .cloud-emx-subtitle,
    .cloud-emx-description,
    .emx-card-title,
    .emx-footer-text,
    .cdr-card-title,
    .cdr-card-subtitle,
    .cdr-card-text,
    .seven-r-title,
    .seven-r-description,
    .seven-r-card-title,
    .seven-r-card-subtitle,
    .cases-pretitle,
    .cases-title,
    .video-description,
    .cta-card-title,
    .cta-card-description,
    .section-title,
    .section-subtitle,
    .footer-column-title,
    .footer-links a,
    .footer-copyright,
    .footer-legal,
    .footer-legal a,
    p, h1, h2, h3, h4, h5, h6, li {
      line-height: 1.2 !important;
    }

    /* Reducir espacios en mobile */
    .challenges-section,
    .escala-methodology-section,
    .cloud-done-right-section,
    .seven-r-section,
    .accelerator-section,
    .solutions-section,
    .use-cases-section,
    .cta-section {
      padding-top: 40px !important;
      padding-bottom: 40px !important;
    }
    .methodology-header {
      margin-bottom: 2rem;
    }
    .how-we-do-it {
      margin-bottom: 2rem;
    }
    .cloud-emx-header {
      margin-bottom: 2.5rem;
    }
    .emx-grid {
      margin-bottom: 2.5rem;
    }
    .seven-r-header {
      margin-bottom: 1.5rem;
    }
    .cases-section-wrapper {
      margin-bottom: 2.5rem;
    }
    .cases-header {
      margin-bottom: 1.5rem;
    }

    .hero-wrapper {
      min-height: 90vh;
      padding: 16px;
    }

    .hero-banner {
      padding-top: 80px;
    }

    .hero-container {
      padding: 0 1.5rem;
      min-height: auto;
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-left {
      max-width: 100%;
      gap: 2rem;
    }

    .hero-right {
      position: static;
      align-items: flex-start;
      margin-top: 2rem;
    }

    .hero-title {
      font-size: 32px;
      line-height: 1.2;
      margin-bottom: 2rem;
      max-width: 100%;
    }

    .hero-button {
      width: 100%;
      padding: 1rem 1.5rem;
      font-size: 14px;
      justify-content: center;
      text-align: center;
    }

    .hero-stat-box {
      padding: 0;
      max-width: 100%;
      gap: 1.5rem;
    }

    .hero-stat-text {
      font-size: 18px;
    }

    .container {
      padding: 0 1.5rem;
    }

    .challenges-intro {
      gap: 1.5rem;
    }

    .challenges-intro-title {
      font-size: 28px;
    }

    .challenges-intro-text {
      font-size: 15px;
    }

    .challenges-intro-image {
      height: 250px;
    }

    .challenges-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .challenge-card {
      padding: 1.5rem;
      min-height: 160px;
    }

    .challenge-title {
      font-size: 16px;
    }

    .clients-logos {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem 1.5rem;
      padding-top: 2rem;
      max-width: 400px;
      margin: 0 auto;
    }

    .clients-logos img {
      height: 32px;
      justify-self: center;
    }

    .methodology-header {
      margin-bottom: 3rem;
    }

    .methodology-title {
      font-size: 28px;
    }

    .methodology-subtitle {
      font-size: 15px;
    }

    .how-we-do-it {
      margin-bottom: 3rem;
    }

    .how-title {
      font-size: 20px;
      margin-bottom: 1.5rem;
    }

    .how-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .how-icon {
      width: 48px;
      height: 48px;
    }

    .how-card-title {
      font-size: 16px;
    }

    .how-card-description {
      font-size: 13px;
    }

    .cloud-emx-section {
      padding: 2rem;
    }

    .cloud-emx-title {
      font-size: 24px;
    }

    .cloud-emx-subtitle {
      font-size: 18px;
    }

    .cloud-emx-description {
      font-size: 14px;
    }

    .emx-grid {
      grid-template-columns: 1fr;
    }

    .emx-card {
      padding: 2rem 1.5rem;
      border-right: none;
      border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    }

    .emx-card:nth-child(2n) {
      border-right: none;
    }

    .emx-card:last-child {
      border-bottom: none;
    }

    .emx-icon {
      width: 48px;
      height: 48px;
    }

    .emx-card-title {
      font-size: 14px;
    }

    .emx-footer-text {
      font-size: 12px;
    }

    .cdr-grid {
      grid-template-columns: 1fr;
    }

    .cdr-card-large {
      grid-column: 1;
      grid-row: auto;
    }

    .cdr-card {
      padding: 2rem 1.5rem;
    }

    .cdr-card-title {
      font-size: 18px;
    }

    .cdr-card-subtitle {
      font-size: 18px;
    }

    .cdr-card-text {
      font-size: 14px;
    }

    .seven-r-header {
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .seven-r-title {
      font-size: 28px;
    }

    .seven-r-description {
      font-size: 15px;
    }

    .seven-r-carousel {
      gap: 1rem;
    }

    .seven-r-card {
      min-width: calc(100vw - 3rem);
      max-width: calc(100vw - 3rem);
      min-height: 400px;
      height: 400px;
      padding: 30px;
    }

    .seven-r-card-number {
      font-size: 32px;
    }

    .seven-r-card-title {
      font-size: 32px;
    }

    .seven-r-card-subtitle {
      font-size: 16px;
    }

    .seven-r-card-content {
      max-width: 100%;
    }

    .accelerator-container {
      padding: 0 2rem;
    }

    .accelerator-section {
      padding: 40px 0;
    }

    .cases-header {
      margin-bottom: 2rem;
    }

    .cases-pretitle {
      font-size: 14px;
    }

    .cases-title {
      font-size: 28px;
    }

    .cases-video-grid {
      gap: 1rem;
    }

    .case-video-card {
      min-height: 280px;
    }

    .play-button-circle {
      width: 40px;
      height: 40px;
    }

    .play-button-circle .material-symbols-outlined {
      font-size: 28px;
    }

    .company-logo {
      height: 40px;
      max-width: 100px;
    }

    .video-description {
      font-size: 16px;
      line-height: 20px;
    }

    .cta-card {
      margin-top: 2rem;
      margin-bottom: 2.5rem;
      padding: 0 0.5rem;
    }

    .cta-card-content {
      padding: 2.5rem 1.5rem;
      flex-direction: column;
      align-items: center;
      text-align: center;
      gap: 2rem;
    }

    .cta-card-content .btn {
      align-self: center;
    }

    .cta-card-text {
      text-align: center;
      align-items: center;
      display: flex;
      flex-direction: column;
    }

    .cta-card-title {
      font-size: 26px;
    }

    .cta-card-description {
      font-size: 14px;
      width: 100%;
    }

    .modal-content {
      width: 95%;
    }

    .modal-close {
      top: -40px;
      width: 40px;
      height: 40px;
    }

    .modal-close .material-symbols-outlined {
      font-size: 24px;
    }

    .section-title {
      font-size: 32px;
    }

    .section-subtitle {
      font-size: 16px;
    }

    .challenges-section,
    .solutions-section,
    .use-cases-section,
    .cta-section {
      padding: 40px 0;
    }

    .challenges-grid {
      grid-template-columns: 1fr;
    }

    .solutions-grid {
      grid-template-columns: 1fr;
    }

    .footer-content {
      padding: 40px 30px;
    }

    .footer-main-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .footer-logo-container {
      justify-content: center;
      order: -1;
    }

    .footer-logo {
      max-width: 150px;
    }

    .footer-column {
      text-align: center;
    }

    .footer-social {
      justify-content: center;
    }

    .footer-bottom {
      padding: 30px;
    }
  }
</style>
